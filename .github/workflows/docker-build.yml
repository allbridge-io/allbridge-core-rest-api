name: Docker Build & Push

on:
  release:
    types: [published]

env:
  DOCKER_IMAGE: 'allbridge/io.allbridge.rest-api'
  SDK_VERSION: 'latest'
  SECURITY_SEVERITY: 'CRITICAL,HIGH'
  SECURITY_FAIL_ON_VULN: 'false'

jobs:
  build:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.release.tag_name }}

      - name: Determine release type
        id: release_type
        run: |
          TAG="${{ github.event.release.tag_name }}"
          if [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "is_stable=true" >> $GITHUB_OUTPUT
          else
            echo "is_stable=false" >> $GITHUB_OUTPUT
          fi

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push (stable release)
        if: steps.release_type.outputs.is_stable == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            SDK_VERSION=${{ env.SDK_VERSION }}
          tags: |
            ${{ env.DOCKER_IMAGE }}:${{ github.event.release.tag_name }}
            ${{ env.DOCKER_IMAGE }}:latest
          provenance: false
          sbom: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push (pre-release)
        if: steps.release_type.outputs.is_stable == 'false'
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            SDK_VERSION=${{ env.SDK_VERSION }}
          tags: |
            ${{ env.DOCKER_IMAGE }}:${{ github.event.release.tag_name }}
          provenance: false
          sbom: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image digest
        run: |
          echo "## Docker Image Published" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** \`${{ github.event.release.tag_name }}\`" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.release_type.outputs.is_stable }}" == "true" ]; then
            echo "**Latest:** Yes" >> $GITHUB_STEP_SUMMARY
          else
            echo "**Pre-release:** Yes (no latest tag)" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ env.DOCKER_IMAGE }}:${{ github.event.release.tag_name }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

  container_scanning:
    name: Container Scanning
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Cache Trivy DB
        uses: actions/cache@v5
        with:
          path: ~/.cache/trivy
          key: trivy-db-${{ github.run_id }}
          restore-keys: |
            trivy-db-

      - name: Run Trivy container scanner
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: '${{ env.DOCKER_IMAGE }}:${{ github.event.release.tag_name }}'
          severity: ${{ env.SECURITY_SEVERITY }}
          format: 'sarif'
          output: 'trivy-container-results.sarif'
          exit-code: ${{ env.SECURITY_FAIL_ON_VULN == 'true' && '1' || '0' }}

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-container-results.sarif'
          category: 'container-scanning'

  sbom_generation:
    name: Generate SBOM
    needs: [build]
    runs-on: ubuntu-latest
    if: vars.SCAN_SBOM == 'true'
    steps:
      - name: Cache Trivy DB
        uses: actions/cache@v5
        with:
          path: ~/.cache/trivy
          key: trivy-db-${{ github.run_id }}
          restore-keys: |
            trivy-db-

      - name: Generate SBOM
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: '${{ env.DOCKER_IMAGE }}:${{ github.event.release.tag_name }}'
          format: 'cyclonedx'
          output: 'sbom.cdx.json'

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v6
        with:
          name: sbom-${{ github.event.release.tag_name }}
          path: sbom.cdx.json
          retention-days: 90
